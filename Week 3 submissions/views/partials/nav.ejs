<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark fixed-top">
	<div class="container">
	<li>
<% if (pagename=="Home") {%>
	<a class="nav-link" style="color:orange" href="/">Home</a>
<%}else{%>
	<a class="nav-link" href="/">Home</a>
<%}%>
	</li>
<li>
<% if (pagename=="About") {%>
	<a class="nav-link" style="color:orange" href="/About">About</a>
<%}else{%>
	<a class="nav-link" href="/about">About</a>
<%}%>
	</li>
<li>
<% if (pagename=="Stuff") {%>
	<a class="nav-link" style="color:orange" href="/Stuff">Stuff</a>
<%}else{%>
	<a class="nav-link" href="/stuff">Stuff</a>
<%}%>
	</li>
		
<%if(typeof(sess)!= "undefined" && sess.loggedin == true){%>
			<a class="nav-link" href="/profile">Profile</a>
			<a class="nav-link" href="/logout">Logout</a>
		<%}%>		
	</div>
</nav>	

<% if(typeof(errors)!= "undefined"){%>
	<% for(x in errors){%>
	<br><span>ERRORS:<%=errors[x]%></span>
<%}}%>

<div style ="z-index: 12; display: block; text-align:right;background-color: #333a40; margin-right: 0;padding-right: 20px;color: grey; position:absolute; width: 155px; margin-top: 50px; padding-top:5px; right:0; border-radius: 10px; padding: 11px; font-size: 12px;">
<form action="/login" method="POST">
<input type="text" name="email" placeholder="Email">
<input type="text" name="password" placeholder="Password">
<input type="submit">
</form>
</div>